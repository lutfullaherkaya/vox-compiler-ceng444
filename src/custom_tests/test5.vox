fun getMax(arr, n) {
    var max = arr[0];
    var i;
    for (i = 0; i < n; i = i + 1) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    return max;
}

/**
 * Gets the min if it exists
 */
function getMin(arr) {
    let min = arr[0];
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] < max) {
            min = arr[i];
        }
    }
    return min;
}